/*
For the map method:
- Takes array , does something on the array
- returns the transformed array
*/

const numbers = [1, 2, 3, 4, 5];
const doubled = numbers.map(num => num * 2);
console.log(doubled);